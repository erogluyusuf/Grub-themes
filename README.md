# Depoyu klonlayın
```bash
git clone https://github.com/erogluyusuf/Grub-themes.git
cd Grub-themes
```
# Kurulum betiğini çalıştırın (Otomatik Windows tespiti ve tema ayarı yapar)
```bash
sudo chmod +x install.sh
sudo ./install.sh
```
